package com.example.paintdontfaint.customviews

import android.graphics.Path

class Stroke(color: Int, strokeWidth: Int, path: Path) {

    var color = color
    var strokeWidth = strokeWidth
    var path = path
}